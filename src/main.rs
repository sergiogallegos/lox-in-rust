mod interpreter;
mod vm;

fn main() {
    println!("Lox Interpreter and VM in Rust");
    // Uncomment the line for the phase you are testing
    //interpreter::lox::run();
    //vm::main_vm::run();
}
